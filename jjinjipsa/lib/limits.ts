/*
 * 오픈 테스트 한도 단일 출처.
 *
 * 값이 여러 파일에 흩어져 있으면 한쪽만 바뀌어 뚫린다.
 * 한도를 조정할 때는 여기만 고친다.
 */

/** 계정당 등록 가능한 고양이 수 (오픈 테스트) */
export const MAX_CATS = 3;

/** 한도 초과 시 안내 문구 */
export const MAX_CATS_MESSAGE = `오픈 테스트 기간에는 최대 ${MAX_CATS}마리까지 등록할 수 있어요.`;
